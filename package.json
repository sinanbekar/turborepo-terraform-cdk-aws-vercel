{
  "name": "turborepo-terraform-cdk-aws-vercel",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "esbuild": "^0.14.54",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.7.1",
    "turbo": "^1.4.2"
  },
  "engines": {
    "node": ">=16"
  },
  "packageManager": "pnpm@7.8.0",
  "pnpm": {
    "patchedDependencies": {
      "aws-cdk-lib@2.37.1": "patches/aws-cdk-lib@2.37.1.patch"
    }
  }
}