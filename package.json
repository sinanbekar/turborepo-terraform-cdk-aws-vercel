{
  "name": "turborepo-terraform-cdk-aws-vercel",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "esbuild": "^0.15.5",
    "eslint-config-custom": "workspace:*",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "turbo": "^1.4.3"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "pnpm@7.8.0"
}
